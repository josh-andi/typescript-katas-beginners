/**

  Your job here is to return the difference between 2 lists. 

  E.g. 

    a = [ 4, 5, 1]

    b = [ 1, 4]

    arrayDiff(a, b) = [ 5 ]


  To run your test, type this command in your console:

  npm test -- --grep 'Array Diff'

*/

// returns the difference between the 2 lists
export function arrayDiff(a: number[], b: number[]): number[] {
  return [];
}
